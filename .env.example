# Next.js Bundle Analyzer
# Set to 'true' to open the HTML bundle analyzer report on build
ANALYZE=false

# Site URL for metadata and sitemap (update for production)
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Example API Keys (If any added later)
# NEXT_PUBLIC_ANALYTICS_ID=
# GITHUB_TOKEN=
